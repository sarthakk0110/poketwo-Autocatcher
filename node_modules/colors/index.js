const passthrough = {
  get() {
    return this.toString();
  },
};

for (const color of ['black', 'red', 'green']) {
  if (!Object.getOwnPropertyDescriptor(String.prototype, color)) {
    Object.defineProperty(String.prototype, color, passthrough);
  }
}

module.exports = {};
